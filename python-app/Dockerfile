# Указываем базовый образ
FROM python:alpine

# Создаем директорию образа
WORKDIR /app

# Копируем файлы из текущей директории в директорию образа
COPY . .

# Указываем команду для выполнения
CMD [ "python", "main.py" ]